# RESPONSIVE_WEB_STANDARDS.md

本文件用于约束 Claude Code 在本仓库中进行网页响应式相关分析、修改、重构、修复时的行为。

适用范围：

- `frontend/` 下所有面向 Web 的页面、布局、组件、表单、列表、弹窗、导航与交互
- Expo + React Native Web 场景
- 需要同时兼顾移动端与桌面端的页面

不适用范围：

- 后端逻辑
- 与响应式无关的样式润色
- 未被任务要求的视觉重设计

---

## 1. 目标

所有响应式改动必须优先满足以下目标：

1. 不破坏现有移动端体验
2. 提升 Web 端可用性、可读性与布局稳定性
3. 保持现有架构边界清晰
4. 保持最小改动原则
5. 降低回归风险
6. 与现有 React Native Web 写法一致

---

## 2. 决策优先级

当规则冲突时，按以下优先级执行：

1. 用户当前任务
2. `AGENTS.md`
3. 本文件
4. `CLAUDE.md`
5. 仓库现有代码风格与已有模式
6. 通用最佳实践

---

## 3. 技术上下文

本仓库前端不是传统纯 Web DOM 项目，而是：

- Expo
- React Native
- React Native Web
- TypeScript

因此 Claude Code 在做响应式改动时，必须优先沿用以下现有模式：

- 使用 `useWindowDimensions()` 判断宽度
- 使用 `Platform.OS === 'web'` 处理 Web 特有行为
- 需要浏览器差异处理时优先使用 `.web.tsx`
- 使用 `StyleSheet.create(...)` 或现有样式工厂
- 优先复用现有 `constants`、`styles`、`theme`、`tokens`

除非当前文件已经明确在用 DOM/CSS 方案，否则不要把共享组件直接改造成仅适用于浏览器的实现。

---

## 4. Claude Code 工作流程

在修改任何响应式问题前，Claude Code 必须先完成以下步骤：

1. 阅读对应 screen、component、styles、hook，确认当前布局如何随宽度变化
2. 确认页面是公共页、认证页、后台页，还是壳层布局页
3. 识别当前页面是否已经存在断点常量或宽度分支
4. 确认问题属于以下哪一类：
   - 溢出
   - 挤压
   - 列数不合理
   - 字体或按钮不可读
   - 桌面空间浪费
   - 弹窗或抽屉在 Web 上行为异常
   - 表单在窄屏或宽屏下不可用
5. 先找 root cause，再改代码，不做只遮盖表象的问题修补
6. 优先最小必要改动，不顺手重构整个页面

---

## 5. 响应式实现原则

### 5.1 Mobile First

- 默认先保证移动端成立，再向平板和桌面扩展
- 不允许为了桌面端优化破坏手机端布局、点击区域或滚动行为
- 当移动端已经稳定时，桌面增强应主要通过容器宽度、列数、间距、分栏和导航形态实现

### 5.2 优先布局调整，不优先视觉重做

- 优先调整以下内容：
  - 容器宽度
  - `maxWidth`
  - `minWidth`
  - `flexDirection`
  - `flexWrap`
  - gap / padding / margin
  - 列数
  - 侧栏/抽屉模式
- 不要在无明确需求时修改品牌色、字体体系、图标风格、文案结构

### 5.3 断点必须收敛

- 优先复用当前模块内已有断点常量
- 如果当前模块没有断点常量，再新增局部常量或具名变量
- 禁止在同一页面散落多个语义接近但不统一的 magic number
- 禁止一边在 JSX 写 `width < 900`，另一边在样式或函数里写 `width <= 920`

### 5.4 Web 增强必须可回退

- Web 专属增强不能让 iOS / Android 端依赖浏览器行为
- 鼠标 hover、cursor、focus ring、sticky、浏览器输入框修正等逻辑，必须限制在 Web 范围
- 涉及 DOM、`document`、`window`、浏览器原生样式覆盖时，必须先判断 `Platform.OS === 'web'`

---

## 6. 断点规范

本仓库已有桌面断点示例：

- `DESKTOP_BREAKPOINT = 1024`
- 某些页面存在 `small / tablet / desktop` 的宽度分支

Claude Code 必须遵守以下规则：

1. 优先复用已有断点，而不是发明新体系
2. 如果页面已经有 `isSmall / isTablet / isDesktop` 模式，优先保持该模式
3. 如果要新增断点，必须给出明确业务语义，而不是只给数字
4. 断点建议最多 3 档：
   - 小屏
   - 中屏
   - 大屏
5. 无明确必要时，不要引入 4 档及以上断点体系

推荐写法：

```ts
const { width } = useWindowDimensions();
const isDesktop = width >= DESKTOP_BREAKPOINT;
const isTablet = width >= 768 && width < DESKTOP_BREAKPOINT;
const isCompact = width < 768;
```

如果当前页面已经有不同阈值，优先尊重现有实现并在原模式上收敛，不要强行替换全页断点。

---

## 7. 布局规范

### 7.1 页面容器

- 页面主内容在 Web 上应有明确内容宽度上限
- 大屏下不要让正文无限拉伸占满整屏
- 优先通过外层容器控制 `maxWidth` 与水平居中
- 页面边距应随宽度变化，但变化必须平滑、可预测

### 7.2 栅格与分栏

- 单列内容在桌面端可升级为双列或主次分栏
- 只有在信息架构清晰时才拆分多列
- 表单、筛选区、信息卡片区允许在桌面端横向展开
- 列表项、卡片组、按钮组在窄屏下必须允许换行或改为纵向堆叠

### 7.3 Flex 约束

- 对可能被压缩的内容，明确设置 `minWidth: 0`
- 对需要保持可点击性的按钮或输入框，谨慎设置 `minWidth`
- 不要依赖固定像素宽高硬撑布局
- 优先使用 `flex`, `gap`, `alignSelf`, `maxWidth`, `width: '100%'`

### 7.4 文本与内容密度

- 长文本必须允许换行，不能因为桌面端布局调整导致截断或溢出
- 标题、说明、标签在中英文场景下都必须考虑长度差异
- 不要通过极小字体来强行塞进窄空间

### 7.5 弹窗、抽屉、浮层

- Web 大屏优先评估是否应该使用居中弹窗，而不是全屏覆盖
- 移动端抽屉不应直接原样照搬到桌面端
- 浮层必须考虑最大宽度、可滚动区域和关闭方式

### 7.6 表格与长列表

- 如果是卡片列表，窄屏下优先单列，宽屏下可增加列数
- 如果是接近表格的信息密度，优先考虑分组卡片或可换行字段布局
- 不要在 Web 上用横向滚动掩盖信息设计问题，除非数据结构本身确实需要

---

## 8. 组件实现规范

### 8.1 JSX 中不要堆响应式逻辑

- JSX 内只允许保留少量具名布尔变量判断
- 复杂宽度分支必须提取到具名变量、样式函数或辅助函数
- 禁止在 JSX 中堆叠多层三元表达式处理布局

不推荐：

```tsx
<View style={{ flexDirection: width < 900 ? 'column' : width < 1200 ? 'row' : 'row' }} />
```

推荐：

```tsx
const isDesktop = width >= DESKTOP_BREAKPOINT;
const contentDirection = isDesktop ? 'row' : 'column';

<View style={[styles.content, { flexDirection: contentDirection }]} />
```

### 8.2 样式组织

- 优先延续当前文件的样式组织方式
- 如果当前组件已经使用独立 `*.styles.ts`，继续放在样式文件里
- 如果当前组件已经使用 `createStyles(theme)` 这类工厂，新增响应式样式时保持同一模式
- 不要为单个响应式修复引入新的全局样式系统

### 8.3 组件职责

- Screen 负责页面编排
- 复杂布局判断可提到 hook 或局部工具函数
- 展示组件只接收已经整理好的状态，不要自己同时处理请求、路由、布局和权限

---

## 9. Web 专属规范

### 9.1 允许的 Web 专属处理

- 浏览器 autofill 样式覆盖
- hover / cursor / focus-visible 增强
- 键盘可访问性增强
- `pointerEvents` 的 Web 差异处理
- 下载、预览、浏览器窗口行为差异处理

### 9.2 何时使用 `.web.tsx`

满足以下任一条件时，可优先考虑 `.web.tsx`：

- 浏览器实现与移动端实现差异明显
- 需要直接使用 Web 特性，且共享实现会变得混乱
- 当前文件已经存在 `.web.tsx` 模式

如果只是少量布局差异，优先在同一组件中通过 `Platform.OS === 'web'` 和宽度判断解决。

### 9.3 禁止行为

- 不要在共享组件中直接使用未保护的 `window`、`document`
- 不要默认依赖鼠标 hover 才能完成核心操作
- 不要只修复 Chrome，不考虑主流浏览器基础可用性
- 不要把 Web 专属 CSS hack 扩散到整个应用

---

## 10. 交互与可访问性规范

- 所有关键操作在窄屏和宽屏下都必须保持可点击
- 点击区建议不小于 44x44
- 表单在 Web 上必须可键盘操作
- `Pressable`、按钮、输入框、关闭按钮、切换器必须有清晰可见的交互状态
- 不要让抽屉、弹窗、浮层在桌面端遮住核心操作且无法快速关闭
- 页面在桌面端展开后，视觉层级必须清楚，不要因为分栏导致用户找不到主要操作区

---

## 11. 数据与架构边界

响应式改动默认只允许调整以下层面：

- 组件结构
- 样式
- 局部布局判断
- 少量与布局直接相关的展示逻辑

默认不应触碰以下内容，除非任务明确要求：

- API 契约
- service 请求逻辑
- DTO / schema / backend
- 权限模型
- 业务流程
- 全局状态结构

不要借响应式改动顺手重构接口、权限、表单数据流或业务逻辑。

---

## 12. 常见问题的推荐修复策略

### 12.1 内容在 Web 上过窄或过宽

- 优先调整页面容器 `maxWidth`
- 为主要内容区增加中心容器
- 不要直接把所有子元素宽度都改成固定值

### 12.2 卡片在窄屏挤压变形

- 优先改为纵向堆叠
- 增加 `flexWrap`
- 检查是否缺少 `minWidth: 0`
- 检查按钮组是否应该换行

### 12.3 桌面端空间浪费

- 优先增加双列布局、侧栏信息区或更合理的内容宽度
- 不要仅仅放大字体和间距来“填满屏幕”

### 12.4 表单在 Web 上不好用

- 增加分组、分栏和字段宽度上限
- 确保错误提示不会挤压输入框导致跳动
- 提交按钮位置必须稳定、可预期

### 12.5 抽屉或导航在桌面端不自然

- 优先评估固定侧栏或常驻导航
- 保留移动端抽屉逻辑，但不要让桌面端仍依赖遮罩打开侧栏

---

## 13. 禁止事项

Claude Code 在响应式修改中禁止做以下事情：

1. 未经要求引入新的 UI 框架、响应式库、CSS-in-JS 方案或第三方布局依赖
2. 未经要求把现有 `StyleSheet` 体系整体迁移到 CSS / SCSS
3. 为了修一个页面，顺手批量改多个无关页面断点
4. 用大量硬编码像素值堆出桌面布局
5. 在 JSX 内联复杂宽度判断和大段样式对象
6. 通过删除功能、删文案、缩小字体来回避布局问题
7. 用 `overflow: hidden` 或强制裁切掩盖本质溢出问题
8. 未确认影响范围就修改全局 layout、导航壳层或共享基础组件
9. 伪造已验证结论

---

## 14. 验收标准

完成响应式改动后，至少应从以下角度自检：

1. 小屏是否可用
2. 中屏是否过渡自然
3. 大屏是否仍然有清晰的信息层级
4. 主要按钮是否始终可见、可点
5. 文本是否溢出、截断、重叠
6. 弹窗、抽屉、浮层是否符合当前屏幕尺寸
7. 是否引入了新的平台分支风险
8. 是否保持了现有业务逻辑与接口不变

建议至少按以下宽度思考验证：

- 390
- 768
- 1024
- 1280 或 1440

如果无法实际运行页面，也必须明确说明“基于代码审查完成静态判断，未做实际页面验证”。

---

## 15. 输出要求

Claude Code 完成响应式相关任务后，输出必须包含：

1. 修改了什么
2. 为什么这样改
3. 影响范围
4. 风险点
5. 验证方式
6. 若未执行测试，明确写出：`未执行测试`

不得声称以下内容已完成，除非确实执行过：

- 页面已实机验证
- Web 端已验证通过
- 所有断点均已验证
- 测试通过
- 构建通过
- lint 通过

---

## 16. Claude Code 执行指令

当任务涉及“网页响应式”“Web 适配”“桌面端布局”“平板布局”“大屏优化”“窄屏修复”时，Claude Code 必须默认按以下方式执行：

1. 先阅读相关 screen、component、styles、hook
2. 总结当前断点和布局实现
3. 判断问题属于布局、交互还是平台差异
4. 采用最小必要改动修复
5. 优先复用现有断点与样式模式
6. 不做无关重构
7. 最后按本文件的输出要求汇报

---

## 17. 一句话原则

响应式改动的核心不是“把页面拉伸到不同宽度都能显示”，而是：

在不破坏现有架构和移动端体验的前提下，让 Web 页面在不同宽度下都保持清晰、稳定、可用、可维护。
