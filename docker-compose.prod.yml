services:
  backend:
    image: ${IMAGE:?IMAGE is required}:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "${BACKEND_PORT:-3000}:3000"
    volumes:
      - "${STORAGE_ROOT_PATH:-/data/storage}:/data/storage"
